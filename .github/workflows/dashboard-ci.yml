name: Dashboard CI

on:
  push:
    paths:
      - "harness-cli/**"
      - "docs/harness-cli-*.md"
      - "workshop-skill/**"
      - "dashboard/**"
      - ".github/workflows/dashboard-ci.yml"
      - ".github/workflows/harness-cli-publish.yml"
  pull_request:
    paths:
      - "harness-cli/**"
      - "docs/harness-cli-*.md"
      - "workshop-skill/**"
      - "dashboard/**"
      - ".github/workflows/dashboard-ci.yml"
      - ".github/workflows/harness-cli-publish.yml"

permissions: read-all

concurrency:
  group: dashboard-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify-harness-cli:
    name: verify-harness-cli (${{ matrix.name }}, Node ${{ matrix.node_version }})
    runs-on: ${{ fromJSON(matrix.runs_on) }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: ubuntu-latest
            runs_on: '"ubuntu-latest"'
            node_version: 22
          - name: windows-latest
            runs_on: '"windows-latest"'
            node_version: 22
    defaults:
      run:
        working-directory: harness-cli
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: ${{ matrix.node_version }}
          cache: npm
          cache-dependency-path: harness-cli/package-lock.json

      - run: npm ci

      - run: npm run verify:workshop-bundle

      - name: Run tests
        run: npm test

      - name: Generate coverage report
        if: matrix.name == 'ubuntu-latest'
        run: node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage.lcov

      - name: Upload coverage to Codecov
        if: matrix.name == 'ubuntu-latest'
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2 # v6
        with:
          files: harness-cli/coverage.lcov
          flags: harness-cli
          fail_ci_if_error: false

      - run: npm pack

      - name: Install packed CLI tarball
        shell: bash
        run: |
          TARBALL="$(ls -1 *.tgz | head -n 1)"
          mkdir -p ../tmp/harness-cli-smoke
          cd ../tmp/harness-cli-smoke
          npm init -y
          npm install "../../harness-cli/${TARBALL}"

      - name: Run CLI help smoke test
        shell: bash
        run: |
          ../tmp/harness-cli-smoke/node_modules/.bin/harness --help

      - name: Run portable skill install smoke test
        shell: bash
        run: |
          mkdir -p ../tmp/harness-cli-target-repo
          ../tmp/harness-cli-smoke/node_modules/.bin/harness skill install --target ../tmp/harness-cli-target-repo

      - name: Run portable skill reinstall smoke test
        shell: bash
        run: |
          ../tmp/harness-cli-smoke/node_modules/.bin/harness skill install --target ../tmp/harness-cli-target-repo

  verify-harness-cli-self-hosted:
    name: verify-harness-cli (linux-self-hosted, Node 25)
    if: github.event_name == 'push'
    timeout-minutes: 10
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    defaults:
      run:
        working-directory: harness-cli
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 25
          cache: npm
          cache-dependency-path: harness-cli/package-lock.json

      - run: npm ci

      - run: npm run verify:workshop-bundle

      - run: npm test

      - run: npm pack

      - name: Install packed CLI tarball
        shell: bash
        run: |
          TARBALL="$(ls -1 *.tgz | head -n 1)"
          mkdir -p ../tmp/harness-cli-smoke
          cd ../tmp/harness-cli-smoke
          npm init -y
          npm install "../../harness-cli/${TARBALL}"

      - name: Run CLI help smoke test
        shell: bash
        run: |
          ../tmp/harness-cli-smoke/node_modules/.bin/harness --help

      - name: Run portable skill install smoke test
        shell: bash
        run: |
          mkdir -p ../tmp/harness-cli-target-repo
          ../tmp/harness-cli-smoke/node_modules/.bin/harness skill install --target ../tmp/harness-cli-target-repo

      - name: Run portable skill reinstall smoke test
        shell: bash
        run: |
          ../tmp/harness-cli-smoke/node_modules/.bin/harness skill install --target ../tmp/harness-cli-target-repo

  verify-dashboard:
    name: verify-dashboard
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: dashboard
    permissions:
      contents: read
    env:
      HARNESS_TEST_DATABASE_URL: ${{ secrets.HARNESS_TEST_DATABASE_URL }}
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci

      - run: npm test

      - run: npm run lint

      - run: npm run build

      - if: env.HARNESS_TEST_DATABASE_URL != ''
        run: npm test -- neon-runtime.integration.test.ts

      - if: env.HARNESS_TEST_DATABASE_URL != ''
        run: npm test -- neon-auth.integration.test.ts

  e2e-dashboard:
    name: e2e-dashboard (pull_request, hosted)
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    needs:
      - verify-dashboard
    defaults:
      run:
        working-directory: dashboard
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci

      - run: npm run build

      - run: npx playwright install --with-deps chromium

      - run: npm run test:e2e

  e2e-dashboard-self-hosted:
    name: e2e-dashboard (push, self-hosted)
    if: github.event_name == 'push'
    timeout-minutes: 15
    runs-on:
      - self-hosted
      - Linux
      - ARM64
    needs:
      - verify-dashboard
    defaults:
      run:
        working-directory: dashboard
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: dashboard/package-lock.json

      - run: npm ci

      - run: npm run build

      - run: npx playwright install chromium

      - run: npm run test:e2e

  secret-scan:
    name: secret-scan
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      GITLEAKS_VERSION: "8.30.1"
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          fetch-depth: 0

      - name: Install gitleaks
        shell: bash
        run: |
          curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${GITLEAKS_VERSION}/gitleaks_${GITLEAKS_VERSION}_linux_x64.tar.gz" \
            | tar -xz -C /tmp gitleaks

      - name: Scan git history for secrets
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          GITHUB_SHA: ${{ github.sha }}
          PUSH_BEFORE_SHA: ${{ github.event.before }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          ZERO_SHA="0000000000000000000000000000000000000000"

          if [ "${EVENT_NAME}" = "pull_request" ] && [ -n "${PR_BASE_SHA}" ] && [ -n "${PR_HEAD_SHA}" ]; then
            LOG_OPTS="${PR_BASE_SHA}..${PR_HEAD_SHA}"
          elif [ -n "${PUSH_BEFORE_SHA}" ] && [ "${PUSH_BEFORE_SHA}" != "${ZERO_SHA}" ]; then
            LOG_OPTS="${PUSH_BEFORE_SHA}..${GITHUB_SHA}"
          elif git rev-parse "${GITHUB_SHA}^" >/dev/null 2>&1; then
            LOG_OPTS="$(git rev-parse "${GITHUB_SHA}^")..${GITHUB_SHA}"
          else
            LOG_OPTS="${GITHUB_SHA}"
          fi

          /tmp/gitleaks git --exit-code 1 --log-opts="${LOG_OPTS}" .

  sast-semgrep:
    name: sast-semgrep
    runs-on: ubuntu-latest
    permissions:
      contents: read
    container:
      image: semgrep/semgrep
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - run: semgrep scan --config auto --error dashboard/app dashboard/lib dashboard/scripts harness-cli/bin harness-cli/src harness-cli/scripts

  dependency-review:
    name: dependency-review
    if: github.event_name == 'pull_request' && (!github.event.repository.private || vars.ENABLE_DEPENDENCY_REVIEW == 'true')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - uses: actions/dependency-review-action@2031cfc080254a8a887f58cffee85186f0e49e48 # v4.9.0

  audit-packages:
    name: audit-packages (${{ matrix.package }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        package:
          - dashboard
          - harness-cli
    defaults:
      run:
        working-directory: ${{ matrix.package }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: ${{ matrix.package }}/package-lock.json

      - run: npm ci

      - run: npm audit --audit-level=high

  deploy-ready:
    name: deploy-ready
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      statuses: write
    needs:
      - verify-dashboard
      - verify-harness-cli
      - verify-harness-cli-self-hosted
      - e2e-dashboard
      - e2e-dashboard-self-hosted
      - secret-scan
      - sast-semgrep
      - dependency-review
      - audit-packages
    steps:
      - name: Assess aggregate CI result
        id: assess
        env:
          EVENT_NAME: ${{ github.event_name }}
          VERIFY_RESULT: ${{ needs.verify-dashboard.result }}
          CLI_RESULT: ${{ needs.verify-harness-cli.result }}
          CLI_SELF_HOSTED_RESULT: ${{ needs.verify-harness-cli-self-hosted.result }}
          E2E_PR_RESULT: ${{ needs.e2e-dashboard.result }}
          E2E_PUSH_RESULT: ${{ needs.e2e-dashboard-self-hosted.result }}
          SECRET_SCAN_RESULT: ${{ needs.secret-scan.result }}
          SAST_SEMGREP_RESULT: ${{ needs.sast-semgrep.result }}
          DEPENDENCY_REVIEW_RESULT: ${{ needs.dependency-review.result }}
          AUDIT_PACKAGES_RESULT: ${{ needs.audit-packages.result }}
        run: |
          echo "event-name: ${EVENT_NAME}"
          echo "verify-harness-cli: ${CLI_RESULT}"
          echo "verify-harness-cli-self-hosted: ${CLI_SELF_HOSTED_RESULT}"
          echo "verify-dashboard: ${VERIFY_RESULT}"
          echo "e2e-dashboard (pull_request, hosted): ${E2E_PR_RESULT}"
          echo "e2e-dashboard (push, self-hosted): ${E2E_PUSH_RESULT}"
          echo "secret-scan: ${SECRET_SCAN_RESULT}"
          echo "sast-semgrep: ${SAST_SEMGREP_RESULT}"
          echo "dependency-review: ${DEPENDENCY_REVIEW_RESULT}"
          echo "audit-packages: ${AUDIT_PACKAGES_RESULT}"

          STATE="success"
          DESCRIPTION="Ready for Vercel promotion"

          if [ "${CLI_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="verify-harness-cli failed"
          fi

          if [ "${VERIFY_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="verify-dashboard failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${EVENT_NAME}" = "push" ] && [ "${CLI_SELF_HOSTED_RESULT}" = "failure" ]; then
            STATE="failure"
            DESCRIPTION="verify-harness-cli-self-hosted failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${EVENT_NAME}" = "pull_request" ] && [ "${E2E_PR_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="e2e-dashboard failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${EVENT_NAME}" = "push" ] && [ "${E2E_PUSH_RESULT}" = "failure" ]; then
            STATE="failure"
            DESCRIPTION="e2e-dashboard-self-hosted failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${SECRET_SCAN_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="secret-scan failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${SAST_SEMGREP_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="sast-semgrep failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${DEPENDENCY_REVIEW_RESULT}" != "success" ] && [ "${DEPENDENCY_REVIEW_RESULT}" != "skipped" ]; then
            STATE="failure"
            DESCRIPTION="dependency-review failed"
          fi

          if [ "${STATE}" = "success" ] && [ "${AUDIT_PACKAGES_RESULT}" != "success" ]; then
            STATE="failure"
            DESCRIPTION="audit-packages failed"
          fi

          echo "state=${STATE}" >> "$GITHUB_OUTPUT"
          echo "description=${DESCRIPTION}" >> "$GITHUB_OUTPUT"

      - name: Publish Vercel promotion status
        if: always()
        uses: actions/github-script@ed597411d8f924073f98dfc5c65a23a2325f34cd # v8
        with:
          script: |
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha: context.sha,
              state: '${{ steps.assess.outputs.state }}',
              context: 'Vercel - harness-lab-dashboard: deploy-ready',
              description: '${{ steps.assess.outputs.description }}',
            });

      - name: Fail if aggregate CI failed
        if: steps.assess.outputs.state != 'success'
        run: exit 1
